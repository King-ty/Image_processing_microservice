syntax = "proto3";

package image_processing;

service BlurService {
    rpc BlurImage (BlurRequest) returns (BlurResponse);
}

message BlurRequest {
    bytes image_data = 1;
    float sigma = 2;
}

message BlurResponse {
    bytes image_data = 1;
}
